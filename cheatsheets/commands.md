## 🟦 GitHub CLI (gh) Commands
| Command | Explanation |
|--------|-------------|
| `gh auth status` | Shows which GitHub account you are logged into via GitHub CLI. |
| `gh auth login` | Logs in to GitHub CLI (fixes auth or lets you switch accounts). |

---

## 🟧 Git Configuration Commands
| Command | Explanation |
|--------|-------------|
| `git config user.name` | Shows the username for commits (repo level). |
| `git config user.email` | Shows the email for commits (repo level). |
| `git config --global --list` | Shows global Git settings like username, email, editor, etc. |
| `git config --global user.name "subzer0"` | Sets global Git username for all repos. |
| ` ` | Sets global Git email for all repos. |

---

## 🟩 Git Basic Workflow Commands
| Command | Explanation |
|--------|-------------|
| `git add .` | Stages all modified files for the next commit. |
| `git commit -m "message"` | Creates a commit with your message. |
| `git push origin main` | Pushes commits to the `main` branch on GitHub. |

---

## 🟪 Jekyll / Bundler Commands
| Command | Explanation |
|--------|-------------|
| `bundle exec jekyll serve --livereload` | Runs your Jekyll site locally with auto-refresh on file changes. |
    